#!/usr/bin/python3 -ISB
"""Test stub: prints the NTP state (`yes` unless FAKESYS_NTP or config.json says otherwise)."""
import os
import sys

sys.dont_write_bytecode = True
sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

import fakesys  # noqa: E402

raise SystemExit(fakesys.main("timedatectl", sys.argv))
